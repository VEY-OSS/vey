---

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
  ignore_errors: true

- name: Install proxy package
  ansible.builtin.apt:
    name: vey-proxy
    state: latest
  notify: Systemd daemon reload
