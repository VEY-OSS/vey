.. _log_resolve_fail_over:

*********
fail-over
*********

Error logs generated by resolvers of type ``fail-over``.

Most fields in this log correspond directly to resolver configuration.

next_primary
------------

**required**, **type**: string

The configured primary next resolver.

next_standby
------------

**required**, **type**: string

The configured standby next resolver.
